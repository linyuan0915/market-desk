# 行情分析工作台需求文档

## 项目定位

构建一个面向金融行业工作者、A 股研究员、交易员和关注市场行情用户的行情分析工作台。当前已接入“市场宽度”“每日行情”“A股指数”“市场情绪”“自选观察”“本地数据中心”“跨市场风险”和“板块资金热力图”模块，后续继续围绕市场资金、情绪、趋势、宏观指标和热门商品扩展。

## 当前模块：市场宽度

基于已安装的 `market-breadth-heatmap-skill` 生成并展示 A 股行业市场宽度数据，用于观察一级行业 MA20 站上率变化和市场强弱轮动。

### 数据来源

- 数据源：大盘云图 `https://sckd.dapanyuntu.com/`
- 接口：`/api/api/industry_ma20_analysis_page?page=0`
- 更新频率：每日 16:00 后
- 原始数据：86 个二级行业、约 28-31 个交易日、MA20 站上率

### 计算逻辑

- 将 86 个二级行业映射聚合为 26 个一级行业。
- 对同一一级行业下的二级行业 MA20 站上率取算术平均。
- 过滤 0 值，避免将无数据当作真实 0%。
- 一级行业按最新交易日 MA20 站上率降序排列。

## 当前模块：每日行情

基于本地 `daily-market-brief` skill 所属的每日简报项目，读取或生成中文多资产市场行情简报，并在工作台中以网页文章形式展示。

### 数据与产物

- Skill 路径：`/Users/liuyishan/Desktop/Trae培训文件/市场行情分析web/每日简报/skills/daily-market-brief`
- 简报输出目录：`/Users/liuyishan/Desktop/Trae培训文件/市场行情分析web/每日简报/output`
- 当前复用构建脚本：`/Users/liuyishan/Desktop/Trae培训文件/市场行情分析web/每日简报/tmp/docx/build_5_15_brief.py`
- 后端读取最新 `.docx` 文件，解析标题、交易日期、段落、项目符号和表格，再返回给前端渲染。

### 展示逻辑

- 左侧导航新增“每日行情”入口。
- 顶部状态栏在每日行情页切换为：简报日期、内容板块、更新时间。
- 页面主体显示简报标题、交易日期、更新时间、来源 Skill、正文段落和数据表。
- 提供“生成简报”按钮，调用本地构建脚本重新生成 Word 并刷新页面内容。
- 提供“下载 Word”按钮，下载最新生成的简报文件。

## 全局数据源约定

### A 股基础行情

后续项目涉及 A 股基础行情数据时，可调用本机已配置的 `RssCast` MCP，作为 A 股 open/high/low/close/volume/amount 等日频基础行情数据源之一。

Codex 本机 MCP 配置：

- MCP 名称：`RssCast`
- Transport：`streamable_http`
- URL：`https://app-cn.rsscast.io/api/mcp/v1/mcp`
- 鉴权方式：Bearer Token
- Token 读取环境变量：`RSSCAST_MCP_TOKEN`

运行原则：

- A 股分析优先使用可验证的结构化数据源；若结构化字段缺失，应明确提示缺口，不得补造。
- 日频收盘数据优先，不追求实时高频。
- 若后续同一指标存在多个数据源，应保留来源、日期和字段口径，避免不同平台口径混用。

## 当前模块：A股指数

基于 `RssCast` MCP 读取 A 股核心指数日频行情，形成市场总览和指数强弱观察。

### 覆盖指数

- 上证指数：`000001`
- 深证成指：`399001`
- 创业板指：`399006`
- 沪深300：`000300`
- 中证500：`000905`
- 科创50：`000688`

### 展示逻辑

- 指数卡片：最新点位、涨跌幅、成交额。
- 市场状态：上涨/下跌指数数量、最强指数、最弱指数、上证与深成指成交额合计。
- 指数走势对比：近 90 自然日内交易日 K 线，计算近 20 日涨跌幅和风格强弱。
- 行情明细表：展示 open/high/low/close、涨跌额、涨跌幅、成交额和振幅。

## 当前模块：自选观察

基于 `RssCast` MCP 读取用户自选股日频行情，支持添加、删除和查看个股近一年 K 线。

### 展示逻辑

- 自选池默认包含贵州茅台、平安银行、宁德时代、中国平安。
- 用户可输入 6 位股票代码、可选中文名称和自定义分类添加自选股。
- 若重复添加同一股票代码，则更新该股票的名称和分类，便于补录中文名或调整观察分组。
- 自选池顶部展示分类筛选按钮，支持按“全部/核心资产/金融地产/新能源/未分类/自定义分类”等维度观察。
- 自选表展示最新价、涨跌幅、成交额、换手率、振幅和行情时点。
- 点击“详情”后读取近一年日 K，计算 MA5、MA20、近 20 日涨跌幅，并生成简要技术状态。
- 自选股列表缓存到本地 `output/watchlist.json`。

## 本地 SQL 数据库现状

用户指定的本地 MySQL 数据库为 `market_data`，当前可用表为 `daily_data`。

### daily_data 字段

- `date`：交易日期
- `code`：证券代码
- `name`：证券名称
- `market`：市场分类，例如 A股个股、A股指数、港股个股、美股个股等
- `close`：收盘价
- `change`：涨跌额
- `change_pct`：涨跌幅
- `volume`：成交量
- `amount`：成交额

### 当前数据覆盖

- 当前记录数：7131 行
- 日期范围：2026-04-15 至 2026-05-15
- 标的数量：352 个
- A 股个股：300 只，约 20 个交易日
- A 股指数：7 个，约 20 个交易日
- 同时包含港股个股、港股指数、美股个股、美股指数和美股波动率数据

该库目前适合支撑短周期横截面观察、涨跌榜、成交额异动、跨市场风险偏好和自选池名称补全；若要做近一年趋势、长周期拥挤度或历史回测，需要继续补齐更长历史。

### RssCast 近一年补数

新增脚本 `scripts/update_a_share_year.py`，用于通过 RssCast MCP 将本地已有 A 股股票池的最近一年日线慢速写入 `market_data.daily_data`。

运行原则：

- 默认读取 `daily_data` 中现有 `A股个股` 且带 `.SZ/.SS` 后缀的股票池。
- 调用 RssCast `StockKLineQuery`，默认近 365 天。
- 默认 5 只股票一批，每批间隔 2 秒，降低频率限制风险。
- 写入时保留原 SQL 代码格式，例如 `000001.SZ`、`600519.SS`。
- 使用 `(date, code)` 唯一键 upsert，避免重复堆积数据。
- 若 RssCast K 线未提供涨跌幅，则按相邻收盘价补算 `change` 与 `change_pct`；每只股票第一条记录允许缺失涨跌幅。

运行命令：

```bash
python3 scripts/update_a_share_year.py --batch-size 5 --sleep 2
```

## 当前模块：本地数据中心

用于回答“当前 SQL 数据够不够用、缺什么、能支撑什么分析”。

### 展示逻辑

- 展示 `market_data.daily_data` 总行数、标的数、市场分类数、最早/最新日期。
- 按 `market` 展示行数、标的数、日期范围、缺失 close、缺失 change_pct、缺失 amount。
- 生成数据质量状态：可用、历史偏短、字段缺失、样本较少。
- 给出后续补数建议，例如 A 股个股池是否足够、港美与 VIX 是否可用于跨市场风险。

## 当前模块：跨市场风险

基于本地 SQL 已有港股指数、美股指数、美股波动率和 A 股指数，形成 A 股开盘前外围环境面板。

### 数据口径

- 数据源：本地 MySQL `market_data.daily_data`
- 字段：`date/code/name/market/close/change_pct/amount`
- 市场范围：`A股指数`、`港股指数`、`美股指数`、`美股波动率`
- 时间范围：接口默认读取最近约 45 个自然日内的本地 SQL 数据，页面展示最新日期的外围风险状态，并保留历史序列供后续趋势图使用。
- 更新频率：取决于本地 SQL 入库频率；当前属于日频/收盘后或盘后同步数据，不做高频实时判断。

### 展示逻辑

- 生成 `0-100` 外围风险分，分数越高代表外围压力越大。
- 重点观察港股指数、美股三大指数、VIX/波动率代理。
- 输出风险状态：外围高压、外围偏谨慎、外围中性、外围风险缓和。
- 展示关键风险信号，例如港美主要指数平均涨跌幅、VIX 涨跌幅、港股科技风险偏好。

## 当前模块：板块资金热力图

新增 A 股板块主力资金热力图，重点观察市场资金流向，而不是做量化回测。

### 数据来源与口径

- 优先调用 `akshare.stock_sector_fund_flow_rank`，其底层口径来自东方财富板块资金流。
- 若东方财富接口不可用，行业/概念资金流尝试调用同花顺资金流接口作为备用源，并在 `source` 字段中明确标注。
- 当前稳定支持：
  - 行业资金流
  - 概念资金流
  - 地域资金流
- 风格资金流暂不混用风格指数或人工分类替代；若没有同口径主力资金字段，页面明确显示“暂未接入”。
- 时间频率：当前资金热力图为即时/当日维度，依赖公开网页接口返回口径；不是本地历史库，不做回测。
- 时间范围：热力图展示接口当前返回的最新一批板块资金数据。

### 展示逻辑

- 热力图面积按主力净额绝对值放大。
- 红色表示主力净流入，绿色表示主力净流出。
- 表格展示板块名称、主力净额、净占比、板块涨跌幅。
- 接口不可用时明确显示数据暂不可用，不伪造资金数据。

### 资金主线追踪

- 新增 `GET /api/fund-mainline?kind=industry|concept`。
- 目标：把行业/概念资金连续 3 日、5 日净流入做成主线榜。
- 数据源优先级：
  - 优先使用同花顺行业/概念 3 日、5 日、10 日阶段资金榜，通过 `akshare.stock_fund_flow_industry` 与 `akshare.stock_fund_flow_concept` 获取。
  - 若阶段资金榜不可用，再尝试东方财富行业/概念历史资金流，通过 `akshare.stock_sector_fund_flow_hist` 和 `akshare.stock_concept_fund_flow_hist` 获取。
- 时间频率：阶段资金榜为 3/5/10 交易日滚动窗口；东方财富历史资金流为日频。
- 时间范围：阶段榜读取 3 日、5 日、10 日；历史流失败回退时读取候选板块最近 5 个交易日。
- 计算逻辑：
  - `net3_yi`：最近 3 个交易日主力净流入合计。
  - `net5_yi`：最近 5 个交易日主力净流入合计。
  - `streak`：阶段榜模式下表示 3/5/10 日中净流入为正的阶段数；历史流模式下表示从最新交易日起连续净流入天数。
  - 阶段榜要求近 3 日与近 5 日主力净流入同时为正，排序优先看为正阶段数，其次看 5 日与 3 日净流入规模。
- 若阶段榜和历史资金流都不可用，应明确提示，不使用即时资金替代连续主线。

## 当前模块：宏观商品

用于观察影响 A 股风险偏好、资金成本、资源品定价和人民币资产压力的宏观变量。

### 数据来源与频率

- 美国 10Y/2Y 国债、美元指数、美元/人民币：FRED CSV 日频数据。
- 中国 10Y 国债：`akshare.bond_zh_us_rate`，日频，默认读取近 180 天。
- 中美 10Y 利差：由中国 10Y 国债与 FRED 美国 10Y 国债计算。
- 热门商品：`akshare.futures_main_sina` 国内期货主连日频数据。
- 关注指标：
  - 美元指数
  - 美元/人民币
  - 中国 10Y 国债
  - 美国 10Y 国债
  - 美国 2Y 国债
  - 中美 10Y 利差
  - 黄金
  - 原油
  - 铜
  - 铁矿石
  - 螺纹钢

### 展示逻辑

- 指标卡片展示最新值、变化、频率/日期和可用状态。
- 明细表展示每条指标的数据来源，便于核对口径。
- 汇总区输出宏观商品对 A 股的风险提示，例如美债上行、人民币走弱、原油上行、铜价走强等。
- 接口不可用时只显示不可用原因，不补造宏观或商品价格。

## 当前模块：市场情绪

市场情绪模块用于回答交易层面的核心问题：今天 A 股是在加风险、降风险，还是结构性切换。

### P0 功能

- 生成 `0-100` 的 A 股情绪温度计。
- 输出交易状态标签，例如：全面 Risk-on、结构性 Risk-on、分化轮动、高 beta 降温、放量杀跌、缩量观望。
- 输出一句先结论后分析的市场情绪判断。
- 从五个维度拆解评分：
  - 指数动能：核心指数上涨比例、平均涨跌幅、最强/最弱指数。
  - 市场宽度：一级行业 MA20 站上率均值、强势行业数量、弱势行业数量。
  - 成交量能：上证指数与深证成指成交额合计及相对近 20 日均值变化。
  - 风格风险偏好：创业板、科创50、中证500等高 beta 指数相对沪深300、上证指数的强弱。
  - 自选股状态：自选池上涨比例、平均涨跌幅、最强/最弱个股。
- 生成 3-5 条交易解释，强调情绪、宽度、量能、风格和自选池的共同指向。

### P1 基础能力

- 每次刷新市场情绪后，将当日情绪结果写入 `output/sentiment_history.json`。
- 展示本地情绪历史曲线。
- 展示涨跌停情绪：
  - 优先调用东方财富涨跌停池公开接口。
  - 若公开接口未返回有效数据，应明确标记“数据暂不可用”，不得伪造涨跌停数量，也不得纳入情绪总分。
- 展示北向/资金流向：
  - 使用东方财富沪深港通资金接口观察北向、南向资金。
  - 使用东方财富指数资金流接口观察上证指数、深证成指主力净流。
- 展示行业拥挤度：
  - 基于市场宽度 `aggregated_data.json` 计算偏热行业、偏冷行业和行业宽度极差。
  - 用于识别行业过热、冰点、拥挤分化和结构切换。
- 提供初步极值提醒：
  - 情绪进入过热区。
  - 情绪进入偏冷区。
  - 较上一记录明显升温或降温。
  - 当前分数处于近 20 条记录高位或低位。

## 页面结构

当前页面已改造成行情分析工作台雏形：

- 左侧导航：早盘驾驶舱、市场宽度、每日行情、A股指数、市场情绪、自选观察、本地数据中心、跨市场风险、板块资金热力图、宏观商品。
- 顶部状态栏：根据当前模块显示交易日/简报日期、核心指标/内容板块数、更新时间。
- 顶部全局按钮“一键更新数据”：从前端触发后端自动补齐本地 SQL 近期数据。
- 主内容区：早盘驾驶舱展示外围风险、情绪温度、资金主线、宏观压力、自选异动和数据新鲜度；市场宽度页展示交互式市场宽度热力图；每日行情页展示网页化简报；A股指数页展示核心指数看板；市场情绪页展示情绪温度计和交易状态；自选观察页展示自选池和个股详情。
- 自选观察页支持分类录入、分类按钮筛选和同一股票名称/分类更新。
- 每日市场简评：基于最新 `aggregated_data.json` 自动生成，先给结论，再给行业情绪分析。
- 可展开行业层级：点击一级行业行名，可在下方展开该一级行业所属二级行业的 MA20 站上率明细。
- 右侧摘要：最强行业、最弱行业、行业数量、近 5 日相对改善、近 5 日走弱。

## 数据更新策略

目标是让用户尽量只在前端点击刷新，不需要每天手动进入后台补 SQL。

### 前端一键更新

- 新增 `POST /api/data-refresh`。
- 点击顶部“一键更新数据”后，后端执行：
  - A 股指数：调用 RssCast `StockIndexKLineQuery`，写入本地 MySQL `market_data.daily_data`，代码口径统一为 `.SS/.SZ` 后缀。
  - A 股个股：只读取网页自选池 `output/watchlist.json` 中的股票，调用 RssCast `StockKLineQuery`，按 5 只一批、每批约 1.2 秒的节奏慢速补缺口，避免触发频率限制。
  - 已有历史的自选股只补最近缺口；新加入且 SQL 没有历史的自选股补近一年，保证个股详情页可展示趋势。
  - 若某只自选股接口未返回 K 线，更新结果中显示 warning，不影响其他自选股入库。
  - 港股指数、美股指数、VIX：调用 Yahoo Finance chart 日线接口，写入本地 SQL，用于跨市场风险面板。
- 日常补数只围绕网页实际用到的数据，不再扫描 SQL 里已有的 300 只 A 股股票池。
- 接口返回每个任务的写入行数、耗时和数据源警告，前端提示用户更新结果。

### 在线实时/准实时读取

- 市场宽度刷新：调用 `market-breadth-heatmap-skill` 抓取大盘云图并更新 `aggregated_data.json`。
- A 股指数、自选观察、市场情绪中的实时行情：直接调用 RssCast MCP 当前行情接口。
- 板块资金热力图：调用东方财富/同花顺资金流公开接口，不写入 SQL。
- 资金主线追踪：调用同花顺 3/5/10 日阶段资金榜，失败时尝试东方财富历史资金流，不写入 SQL。
- 宏观商品：调用 FRED、AkShare 中国国债收益率和新浪国内期货主连，不写入 SQL。

### 本地 SQL 驱动

- 本地数据中心：完全依赖 `market_data.daily_data`，用于诊断数据覆盖。
- 跨市场风险：读取本地 SQL 的 A 股指数、港股指数、美股指数和 VIX；现在可通过“一键更新数据”自动补齐近期缺口。
- 历史趋势类分析：优先读取已写入 SQL 的历史数据；若发现数据日期落后，应先点击“一键更新数据”。

## 交互功能

- 刷新：调用 skill 重新抓取数据、生成 PNG，并更新 `aggregated_data.json`。
- 下载 PNG：下载报告图片。
- 预览：按需打开 skill 生成的 PNG 图片，不在页面常驻展示。
- 全屏：打开 PNG 大图预览。
- 鼠标悬浮交互式热力图单元格：显示行业、日期和 MA20 站上率。
- 点击一级行业行：展开或收起二级行业明细。

## 后端接口

```http
GET /
GET /api/status
GET /api/market-breadth/data
POST /api/refresh
GET /api/daily-brief/status
POST /api/daily-brief/generate
GET /api/daily-brief/download
GET /api/a-share/indices
GET /api/market-sentiment
GET /api/watchlist
POST /api/watchlist
DELETE /api/watchlist/{code}
GET /api/a-share/stock/{code}
GET /api/data-center
POST /api/data-refresh
GET /api/desk
GET /api/cross-market-risk
GET /api/sector-funds?kind=industry|concept|region|style
GET /api/fund-mainline?kind=industry|concept
GET /api/macro-commodities
```

## 当前实现文件

```text
app/
  main.py
  static/
    index.html
    styles.css
    app.js
output/
  market_breadth_heatmap.png
  raw_data.json
  aggregated_data.json
  market_breadth_heatmap.html
  watchlist.json
  sentiment_history.json
scripts/
  update_a_share_year.py
require.md
requirements.txt
```

## 说明

该版本不再使用 SQLite、本地 Tushare 历史库或涨跌幅 Top10 TreeMap。市场宽度的数据、聚合和 PNG 渲染逻辑由 `market-breadth-heatmap-skill` 提供；Web 页面额外读取 `aggregated_data.json`，生成真正可交互的市场宽度热力图。

每日行情模块当前复用已有 Word 生成工作流。由于 `daily-market-brief` skill 本身只提供写作流程说明，没有通用参数化生成脚本，现阶段“生成简报”调用的是每日简报项目中已有的 `build_5_15_brief.py`。后续若要支持任意交易日期，应将该 skill 的数据采集、写作和 Word 排版流程封装为可传入交易日期的脚本或 MCP 工具。
